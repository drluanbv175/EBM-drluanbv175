# EBM Copilot — Trợ lý AI cho Bác sĩ Y học Bằng chứng

> **Bản gọn — chỉ chứa LUẬT THƯỜNG TRỰC (rút gọn 24/09/2026).** Lịch sử sự cố, số đo theo ngày, mã BH và mọi khối «ĐÍNH CHÍNH» nằm NGUYÊN VĂN ở `audit/NHAT-KY-SU-CO.md` — không nạp mỗi phiên; tra bằng `grep -n "<từ khoá>" audit/NHAT-KY-SU-CO.md`, không đọc cả tệp.
> Sự cố/bài học MỚI ghi vào mục «SAU 24/09/2026» của tệp đó, KHÔNG nối vào đây. Tệp này chỉ nhận LUẬT mới, viết gọn 1–3 dòng; `tools/verify_claude_code_repo_alignment.py` chặn commit khi tệp vượt ngân sách ký tự.
> Mọi SỐ ĐO động (số skill, số hook, số chốt BH, tác vụ lịch, CI, độ tươi, tiến độ đề tài, khoá đã phát) phải ĐO SỐNG bằng lệnh ở §9–§10 — không trích số cũ từ tài liệu.

## Mục tiêu
Hệ thống tự động hóa cho bác sĩ ngoại trú thực hành EBM, 3 module: Research (nghiên cứu), Clinical (lâm sàng), Knowledge (quản lý kiến thức). Thứ tự xây: Research → Knowledge (xuất Obsidian/Anki) → Clinical (RAG guideline + drug check).

## 0. Luật vận hành cốt lõi (mọi phiên)
1. **Ngôn ngữ:** trả lời bác sĩ bằng TIẾNG VIỆT — chỉ thị thường trực, thắng hook `UserPromptSubmit` của `claude-code-harness` ép «Response Language: English». Định danh máy đọc (JSON, mã, tên tệp, tiền tố commit) giữ nguyên. Docstring, comment, thông điệp commit: tiếng Việt.
2. **Đo đúng chỗ trước khi kết luận.** Đọc kết quả một công cụ thì hỏi «nó đã chạy tới luật nào» và «con số này của TẬP HỢP hay của MỘT phần tử». «Không đo được» ≠ «có vấn đề» (không báo đỏ giả) và ≠ «ổn» (không báo xanh giả). Chỉ số gộp (max/min/mới nhất) không bao giờ là kết luận về toàn bộ.
3. **Cây lạc hậu thì chưa được kết luận.** `python3 tools/kiem_cay_lam_viec.py` soi hai repo. Trước khi nói «mã X không tồn tại / chưa vá»: `git fetch origin` rồi `git show origin/<nhánh>:<đường-dẫn>`. Cảnh báo lạc hậu CHẶN việc rút kết luận, không chỉ để in ra.
4. **Tài liệu tổng quan là ảnh chụp.** Khoảng hở của repo nào phải đối chiếu MÃ SỐNG + `Plans.md` của repo đó trước khi báo; khuyến nghị của một bản kiểm toán phải kiểm lại trước khi thi công.
5. **Ranh giới thẩm quyền — agent KHÔNG làm thay bác sĩ:** ký cổng G hay sổ rút bài; phát, đọc, chép khoá riêng (HMAC/Ed25519); đổi `decision` hay nâng `gradeLevel` (chỉ đề xuất; hạ cũng cần bác sĩ duyệt); merge PR; sửa hook được bảo vệ (`.claude/hooks/`); điền nội dung Cổng A (lời dặn bệnh nhân, tiêu chí Annex 2, liều); đổi cài đặt hệ thống/tài khoản (Time Machine, GitHub Actions, môi trường Cloud); dùng `--no-verify`. Sửa hệ thống thì mặc định commit + push lên nhánh làm việc rồi mở PR cho bác sĩ merge. Thay đổi của phiên khác thì để nguyên.
6. **Token:** KHÔNG tự mở Workflow hay nhiều agent song song (một workflow 6 agent ~3 triệu token). Hỏi bác sĩ trước; ưu tiên làm trực tiếp; cần phản biện thì một agent đơn.
7. **OneDrive (Mac↔Windows):** một máy tại một thời điểm; đợi OneDrive xanh rồi mới đổi máy. Trước khi làm: `python3 tools/sync_safety_check.py` (🔴 = DỪNG). Không xoá bản sao « 2» của OneDrive mà không kiểm lại — xoá bản sao từng làm OneDrive xoá luôn bản gốc; dọn xong chạy `git status`, bản gốc mất thì `git checkout -- <đường dẫn>`. Worktree tạm không đặt ở `/private/tmp` (mất khi khởi động lại) — dùng `~/.ebm-worktrees`, commit sớm.
8. **Kiểm đột biến là chuẩn cho mọi chốt mới:** đột biến phải làm chốt đỏ đúng chỗ rồi phục hồi xanh; chạy `python -B` và xoá `__pycache__` trước mỗi phép. Đột biến không bị bắt có thể là lỗi phép thử (nhầm nhánh). Chốt khớp chuỗi phải khớp DÒNG THI HÀNH, không khớp cả tệp (bình luận cùng chuỗi làm xanh giả). Fixture sai thì sửa fixture cho hợp lệ, KHÔNG nới assertion.
9. **Công cụ dùng chung phải chạy trên CẢ Mac lẫn Windows:** không đường dẫn cứng một máy (dùng `Path(__file__)`), gọi Python bằng `sys.executable`, ép UTF-8 cho stdout, rào API chỉ-POSIX (`os.getuid`…). Thêm thư mục/tệp mới ở gốc repo phải thêm dòng `!/<tên>` vào `.gitignore` (luật `/*` bỏ qua âm thầm).

## 1. Điều phối agent — hành vi MẶC ĐỊNH
Đội `.claude/agents/` (~50 agent lâm sàng + nghiên cứu + guardrail dùng chung; bản đồ `.claude/agents/README.md`). Khi bác sĩ nêu việc, MẶC ĐỊNH định tuyến tới «nhạc trưởng» và để nó tự chạy theo Giao thức tự động (không hỏi vặt từng bước):
- **CA lâm sàng** («tôi có bệnh nhân…», hỏi chẩn đoán/điều trị) → `dieu-phoi-lam-sang`: 5 bước EBM (Hỏi→Tìm→Thẩm định→Áp dụng→Theo dõi); **cờ đỏ nêu NGAY**; dừng ở **Cổng A** (áp dụng cho BN) + **Cổng B** (ghi sổ cái). Cổng A/B KHÔNG có lớp mật mã như cổng G — chỉ có kỷ luật vận hành.
- **ĐỀ TÀI nghiên cứu** (chỉ cần tên) → `dieu-phoi-nghien-cuu`: khôi phục trạng thái từ sổ cái → suy thiết kế → march G0→G10; dừng ở 6 cổng cứng (G2 đạo đức · G4 khóa SAP · G5 khóa dữ liệu thật · **G8 bình duyệt độc lập** · G9 liêm chính tác giả · G10 PI khóa gói phát hành) + nơi cần dữ liệu/phê duyệt thật. Mỗi cổng fail-closed theo ĐÚNG vai (`medical-ebm-automation/tools/gate_contract.py`), không chỉ «có ai đó ký». G2 fail-closed khi WHO TRDS v1.3.1 mục 13/14/19/20 thiếu dữ kiện PI đã pin; G9 fail-closed khi thiếu quyền truy cập dữ liệu/độc lập nhà tài trợ (ICMJE 1/2026). Kiểm: `python3 tools/verify_controlled_research_automation.py` — PASS kỹ thuật không thay IRB/PI/thống kê viên/phản biện.
- **VẤN ĐỀ CHỨNG CỨ:** (a) hỏi tại điểm khám → `python3 tools/tra_diem_kham.py "<câu hỏi>"` (chỉ từ thẻ đã duyệt; ngoài phạm vi nói «chưa giám sát»); (b) «cập nhật chứng cứ chủ đề X» → `python3 ops/orchestrator.py --topic "X" --online` (A2 quét → A4 truy nguyên → B2 cổng → B5 hàng chờ; `--cu-nhat N` cho N chủ đề cũ nhất; chủ đề chưa có dashboard thì dừng ở PHIÊN NGƯỜI = skill `cap-nhat-chung-cu-y-khoa`); (c) gói duyệt tuần tự nằm sẵn tối thứ Hai (tác vụ lịch `goi-duyet-tuan-ebm`). Mọi nhánh dừng ở CANDIDATE — Cổng A/B nguyên vẹn.
- **Việc lẻ** (tra 1 câu hỏi, soát TLTK, tính cỡ mẫu, đặc tả biến…) → gọi thẳng agent chuyên trách.
- **Cửa vào** `tools/orchestrator/intent.py` (bảng người đọc: `.claude/agents/README.md` §Ma trận định tuyến). Kind `cong_cu` cho việc có chủ là lệnh/skill. Hai luật ưu tiên NGƯỢC CHIỀU CÓ CHỦ Ý: việc lẻ MẠNH (`VIEC_LE_MANH`) thắng cue ĐỀ TÀI, nhưng KHÔNG BAO GIỜ thắng cue CA LÂM SÀNG (over-route sang nơi có sàng lọc cờ đỏ là chiều an toàn).
- **Tự sửa · tự sinh agent · vòng khép kín:** `tools/tu_sua_chua.py` (mỗi phiên; `--pham-vi-cloud` chỉ chạy mục `chay_tren_cloud`); `.claude/agents/_TU-SINH-AGENT.md` (tuyến lâm sàng chỉ tự sinh khi khoảng trống LẶP LẠI qua nhiều ca; agent mới vẫn qua CÙNG Cổng A/B); `_VONG-LAP-KHEP-KIN.md` mô tả 5 cơ chế cho cả hai tuyến.
- **Chốt kiểm đầu ra (MẶC ĐỊNH):** bước cuối trước khi trả bác sĩ, gọi `tham-dinh-dau-ra`: Lớp 1 LIÊM CHÍNH R1–R7 (nguồn · PII · vượt cổng · tự gán mức · tách 2 trục · nhãn [CẦN…] · disclaimer) + Lớp 2 CHẤT LƯỢNG Med-PaLM Q1–Q7 (gói lâm sàng; `_CHUAN-CHAT-LUONG-MEDPALM.md`). Lỗi đỏ → TRẢ-VỀ-SỬA; Q2/Q5 đỏ → chuyển bác sĩ. Giới hạn: `_KIEM-DUYET-DOC-LAP.md`. `guardrail_bridge` trả `pass` chỉ nói về Lớp 1 — mọi verdict mang `lop_2_medpalm: KHONG_DANH_GIA_QUA_CAU_NAY`.
- **Bất biến:** mỗi đầu ra kèm PMID/DOI + «Cần bác sĩ kiểm chứng»; KHÔNG bịa; KHÔNG PII; agent chỉ ĐỀ XUẤT, bác sĩ duyệt mới «áp dụng».

### 1.1 Plugin — MỘT OWNER
Quyền sở hữu canonical: `.claude/agents/_PLUGIN-ROUTING-CONTRACT.md` + `tools/orchestrator/plugin_ownership_registry.json`. `dieu-phoi-nghien-cuu` sở hữu G0–G10; `dieu-phoi-lam-sang` sở hữu ca ngoại trú. Plugin chỉ là worker đúng allowlist/stage, không tự hợp nhất kết quả, không mở Cổng A/B/G; gọi đích danh plugin chỉ ưu tiên worker, không chuyển owner. Kiểm fail-closed: `python3 tools/verify_plugin_orchestration.py` (plugin chưa cài trên máy này ⇒ ⚪ có khai báo; provider có mà thiếu đúng skill đã khai ⇒ FAIL; tra cả `~/.claude/plugins/cache` lẫn `~/.codex/plugins/cache`).

**Định tuyến khi NHIỀU công cụ cùng nhận một việc — LUẬT BẮT BUỘC:**

| Việc | CHỦ duy nhất (dùng cái này) | Tuyệt đối KHÔNG thay bằng |
|---|---|---|
| Cỡ mẫu / power | `co-mau-nghien-cuu` → `run_g3_auto.py` + `g3_quality_gate.py` | mọi `*sample-size*`, `*power-calculator*` của plugin |
| Thiết kế đề cương / SAP | `thiet-ke-nghien-cuu` → `run_g4_auto.py` + `g4_quality_gate.py` | skill `*-planner`/`*-designer` của aipoch |
| Kiểm trích dẫn | `kiem-chung-trich-dan` + `check_citation_retraction.py` | `reference-integrity-checker`, `verify-refs`, `citation-*` |
| Bình duyệt (G8) | `binh-duyet` + `g8_quality_gate.py` | `sci-paper-reviewer`, `peer-review*` của plugin |
| Tổng quan / gộp | `tong-quan-y-van`, `meta-phan-tich` | `ma-end-to-end`, `systematic-review*`, `meta-*` của aipoch |
| Thống kê | `phan-tich-thong-ke` → `run_stats_analysis.py` | mọi `*statistical-analysis*` của plugin |
| Viết bản thảo | `viet-ban-thao` | skill `*-section-writer`/`*-writer` của aipoch |
| Nộp bài / COI / khai AI | `nop-bai-phan-hoi` + `g9_quality_gate.py` | `cover-letter-*`, `journal-*`, `target-journal-matcher` |
| Khử định danh / PII | `quan-ly-du-lieu` + quy tắc KHÔNG PII | `deidentify*`, `openmed:*deident*` |
| Ca lâm sàng | `dieu-phoi-lam-sang` (dừng Cổng A/B) | mọi skill lâm sàng của plugin |

Ba luật nền: (1) plugin không bao giờ là chủ của việc có cổng — được gọi thì chỉ ở vai worker, đầu ra qua chủ chuẩn hoá rồi qua `tham-dinh-dau-ra`; (2) không có chủ trong bảng → hỏi bác sĩ, không tự chọn plugin; plugin bị cấm ghi `exports/*/approval_ledger*.json`, `study_meta.json`, `EBM_MASTER.json`; (3) `humanizer` KHÔNG chạy trên nội dung y khoa đã qua cổng (làm lệch mệnh đề điều kiện). Hai lối định tuyến skill bổ trợ nhau: `plugin-router-chatgpt` (xếp hạng từ khoá, phủ mọi plugin, cần chạy script) và `dieu-phoi-aipoch` (nhóm ngữ nghĩa cho 605 skill aipoch, văn bản thuần). Không xây thêm router kiểu này cho plugin nhỏ.

## 2. Đồng bộ nhiều nơi chạy (Mac · Windows · Cloud)
- **Một lệnh cho tất cả:** `python3 tools/dong_bo_tat_ca.py [--ap-dung]` (8 làn theo thứ tự phụ thuộc; làn ① an toàn là CỔNG CHẶN; công cụ vắng mặt thì không dừng; KHÔNG tự commit/push).
- **Agent Claude ↔ Codex:** `.claude/agents/*.md` là nguồn biên tập; `.Codex/agents/*.toml` / `.codex/agents/*.toml` là bản sinh. Sau khi sửa/thêm agent: `python3 tools/enforce_agent_guardrails.py` → `python3 tools/sync_agents_to_codex.py` → `python3 tools/sync_agents_to_codex.py --check`; kiểm tổng `python3 tools/audit_ebm_system.py` và `python3 tools/check_claude_codex_sync_health.py`. Thêm luật cho toàn đội = thêm một cặp vào `THAY_THE` của `enforce_agent_guardrails.py` rồi `--refresh`.
- **Pre-commit fail-closed** (`.githooks/pre-commit`, hai repo): chặn khi mirror drift, guardrail chưa đạt, hợp đồng repo lệch, đếm cổng cứng lệch, orchestration plugin hỏng, nhiễm artifact (máy thiếu dữ liệu thật ghi đè artifact — `retracted→unknown` CHẶN, `unknown→retracted` CHO QUA, đừng đảo) và liêm chính `exports/`. Kích hoạt 1 lần/máy: `git config core.hooksPath .githooks && chmod +x .githooks/pre-commit`.
- **Skill nguồn chung:** `sync/skills/` là nguồn DUY NHẤT; `python3 tools/dong_bo_skill_claude_codex.py --ap-dung --dong-bo-plugin` nối symlink sang `~/.claude/skills` + `~/.codex/skills`, dựng catalog/ZIP router, đối chiếu kho plugin (hook `SessionStart` tự chạy). Cowork chỉ nạp Custom Skills của TÀI KHOẢN claude.ai — tệp chép tay vào thư mục Cowork bị app dọn (trần kiến trúc). Chiều đồng bộ quyết theo NỘI DUNG, không theo mtime. Trước khi dời gì khỏi `~/.claude/skills/`, loại trừ mọi tên có trong `sync/skills/` TRƯỚC.
- **Plugin:** `sync/plugin-manifest.json` khai Ý ĐỊNH (plugin nào cần ở máy nào, đi qua git); `tools/moc_chuan_plugin.json` là ảnh chụp TRẠNG THÁI riêng từng máy (`--ghi-moc` riêng mỗi máy, chỉ sau thay đổi có chủ ý). Công cụ KHÔNG tự cài/gỡ plugin qua mạng. Trong `enabledPlugins`: **vắng mặt = BẬT**; muốn tắt phải ghi rõ `false`. App định kỳ xoá các cờ `false` của bộ medsci trùng → `python3 tools/kiem_co_tat_plugin_trung.py --ap-dung` (khôi phục cờ TRƯỚC, ghi mốc SAU). Kiểm kho: `python3 tools/kiem_plugin_day_du.py` (đếm tệp trên đĩa, không phải số skill model thấy). Plugin kiểu `git-thu-cong` chỉ được có `marketplace.json` (thêm `plugin.json` cùng khai component với `strict:false` ⇒ cả plugin không nạp). Claude Code phục vụ skill plugin từ cache `installPath`, không từ bản clone marketplace.
- **Ngân sách danh sách skill:** khai ở `sync/cau-hinh-nguoi-dung.json` (`skillListingBudgetFraction` 0.08 + `skillListingMaxDescChars` 80); app hay xoá → `python3 tools/kiem_cau_hinh_nguoi_dung.py --ap-dung` (chỉ ghi đúng khoá đã khai, sao lưu trước). Đọc settings qua `tools/doc_settings.py` (gộp `settings.json` + `settings.local.json`, bản `.local` đè). `skillOverrides` KHÔNG tác dụng với skill plugin; muốn ẩn skill plugin chỉ có cách tắt hẳn plugin.
- **Hook `SessionStart`:** chốt của máy thật nằm ở `.claude/settings.local.json` (không track); `.claude/settings.json` (track) chỉ chứa hook cloud. Chép hook giữa máy: `tools/dong_bo_hook_sessionstart.py`.
- **Bộ nhớ:** `~/.claude/projects/<mã>/memory/` nằm NGOÀI OneDrive → `python3 tools/sync_memory.py` trên mỗi máy (tệp mới hơn thắng, không xoá). Venv `~/.ebm-venv`, secrets `~/.ebm-secrets`, quyền MCP connector phải làm lại mỗi máy.
- **Git hỏng do OneDrive** (HEAD không giải được, `git fsck` báo missing object): trên máy còn đủ dữ liệu `git bundle create <ten>.bundle --all`; đợi OneDrive xanh; máy thiếu `git fetch <bundle> 'refs/*:refs/rescue/*'` rồi `git fsck --full`.

### 2.1 Phiên Cloud (claude.ai/code)
- `.claude/hooks/session-start.sh` (khai trong `.claude/settings.json`, đi qua git) chỉ chạy khi `CLAUDE_CODE_REMOTE=true`: nối skill, khôi phục ngân sách skill từ `sync/cau-hinh-nguoi-dung.json`, cài thư viện thiếu, sinh lại mirror Codex, tự sửa `--pham-vi-cloud`, cài plugin ở NỀN (`tools/cai_plugin_phien_cloud.py --ap-dung`, từ chối chạy trên máy thật), nạp nền Retraction Watch.
- **NGUYÊN NHÂN GỐC vì sao hook cloud KHÔNG tự bắn khi phiên có ≥2 REPO:** khi một phiên đính kèm nhiều repo, hook cấp DỰ ÁN của từng repo không được quét (hành vi nền tảng; không có gì trong repo để sửa).
- **Chỉ thị đứng:** nếu phiên là CLOUD (`CLAUDE_CODE_REMOTE=true`) VÀ có ≥2 repo trong phiên, tự chạy `bash <gốc-repo-EBM-drluanbv175>/.claude/hooks/session-start.sh` NGAY ĐẦU PHIÊN, trừ khi đã có bằng chứng nó vừa chạy (vd `~/.claude/ebm-cai-plugin-cloud.log` mới). Đường chắc hơn: tách repo này ra một phiên chỉ-một-repo (đã kiểm thực nghiệm: hook tự chạy).
- Engine Python trên Cloud chạy chế độ MOCK cho tới khi bác sĩ đặt Network access + biến môi trường (`USE_MOCK_SOURCES=false`, `NCBI_EMAIL`) cho môi trường Cloud (chi tiết `audit/15-kiem-nguon-chung-cu-tren-cloud_2026-09-24.md`). Không dán khoá vào khung chat.
- `cloud-mirror/trang-thai-chung-cu.json`: tóm tắt trạng thái chứng cứ cho Cloud, sinh bằng `python3 tools/xuat_trang_thai_cloud.py` trên máy có `EBM-Dashboards/`. Bản xung đột OneDrive của nó chuyển vào `cloud-mirror/_quarantine-conflict-copy/`.
- Tên công cụ connector đổi theo nơi chạy — doctrine `_CONNECTOR-CHUNG-CU.md`; máy chủ connector lỗi ⇒ PARTIAL, không đọc thành «không có kết quả».

## 3. Tra công cụ & Việt hoá
- Tra: mở `TRA-CUU-CONG-CU.html` (gõ có/không dấu đều ra) → lệnh `/cong-cu-gi <việc>` (đọc `tools/vietnamize/INDEX-CONG-CU.md`; chưa đủ thì grep `tools/vietnamize/DANH-MUC-CONG-CU.md`, KHÔNG đọc cả tệp).
- Sinh lại sau MỖI lần cài/gỡ/cập nhật plugin, trên CẢ HAI máy: `python3 tools/vietnamize/extract_catalog.py` → `python3 tools/vietnamize/build_danh_muc.py` → `python3 tools/vietnamize/build_trang_tra_cuu.py`. Tệp sửa tay duy nhất: `tools/vietnamize/viec-hay-lam.json`.
- Việt hoá: nguồn bền `tools/vietnamize/vi_descriptions.json` (lớp phủ lúc dựng danh mục/trang tra). Menu gõ `/` đọc thẳng cache plugin nên bản cập nhật plugin trả mô tả về tiếng Anh; `tu_sua_chua.py` chạy `apply_vi.py --tu-quet` mỗi phiên (chạy tay: `~/.ebm-venv/bin/python tools/vietnamize/apply_vi.py --tu-quet`, có hiệu lực ngay). `apply_vi` từ chối ghi khi thiếu PyYAML và từ chối ghi vào tệp nằm trong repo git. Từ điển KHÔNG chứa bản dịch cho skill bác sĩ tự viết (`sync/skills/`). Cloud đè Cowork định kỳ — chỉ phục hồi được, không đẩy ngược được; skill dựng sẵn của app không Việt hoá được.

## 4. Bản đồ dự án (đọc trước khi sửa code)
- **`medical-ebm-automation/` = DỰ ÁN SỐNG** (pipeline EBM, research tracker, dashboard, Evidence Workbench, scheduler, scoring, RAG). Mọi việc lập trình mặc định làm ở đây; đọc `medical-ebm-automation/CLAUDE.md` + `AGENTS.md`. Nhánh mặc định: `feat/r1-1-2-design-gap-remediation`. 32 thang điểm `verified` có nguồn đã rà; chỉ 8 thang có công cụ tính xác định (`tools/risk_score_calc.py`), còn lại là mô tả văn xuôi.
- **`ebm-copilot/`** = nguyên mẫu Phase 1, lưu trữ — không thêm tính năng (cần thì port sang dự án sống).
- Hạ tầng dùng chung: `EBM-Dashboards/`, `dashboard_mockups/`, `EBM_MASTER/`, `sync/`. Dữ liệu cố ý nằm NGOÀI git (code → GitHub, dữ liệu → OneDrive).
- **`clinical_runtime/`** = hợp đồng governance Clinical V2 (schema/static hardening cho C3/C5/C6/C7, retraction, prompt-injection, conflicting evidence); kiểm `python3 tools/verify_clinical_runtime_schema_hardening.py`. KHÔNG gọi là production runtime cho dữ liệu bệnh nhân thật khi chưa có tích hợp, bảo mật, UAT và phê duyệt thật.
- **`Antifacts.html`** = mặt tiền EBM theo chuyên khoa, sinh bằng `tools/build_antifacts.py` (quét glob `EBM-Dashboards/WebDashboard_*.html`; giữ gói ngoài hub thì khai trong `EBM-Dashboards/antifacts-exclude.txt`). Sửa bố cục = sửa generator, không sửa tay HTML.
- `observability/` là sổ QA SỐNG (`APPRAISALS.jsonl`); `governance/`, `assurance/`, `convergence/` là hồ sơ TĨNH. `medical-ebm-automation/chronic-care-clinic-os/` (Next.js) chưa nối vào hệ agent, tự khoá `BLOCKED_FOR_PRODUCTION`, không ghi DB thật, không có code AI.
- **Nhánh mồ côi — KHÔNG phải cổng thật, KHÔNG dời/xoá** (test canh gác chặn cổng thật import chúng): `runtime/policy_gate_engine.py` + `runtime/controlled_orchestrator.py`, `research_studio/`, `research_automation/`, `research_project/` (synthetic-only; `--approval-ledger` chỉ ĐỌC nhưng pha trộn với khung giả), `app/core/approval_service.py` + `app/chronic_care/` (tab 14 dashboard Streamlit chạy thật, read-only), `app/evidence/citation_verification.py` + `retraction_monitor.py` (không tự tra). `runtime/approval_ledger.py` là ledger THẬT.
- **Cổng G THẬT duy nhất:** `tools/gate_contract.py` + `tools/approve_gate.py` + `tools/run_g*_auto.py` của repo y khoa (+ A12 kiểm trích dẫn qua `run_g10_assemble.py` và `check_citation_retraction.py`).
- **Sổ cái phê duyệt có niêm phong:** `exports/<study>/approval_ledger.seal.json` — TUYỆT ĐỐI không xoá, không sửa tay, không bỏ khi sao lưu. Mỗi bản ghi ký cả `prev_hash`; sửa tay `approval_ledger.json` làm mọi cổng của đề tài BỊ CHẶN — thay đổi phải qua `approve_gate.py` (tự niêm phong lại). Bị chặn thì đọc lý do trong thông điệp. Payload chữ ký chứa vai trò (`v2:<phạm-vi>:<hex>`); khoá riêng theo vai `~/.ebm-secrets/gate_approval_key_<NHÓM>`; `EBM_GATE_KEY_PATH` chỉ có tác dụng dưới pytest; `ledger_approved()` fail-closed mặc định (ngoại lệ `study_kind=synthetic_test`); `run_g10_assemble.py --i-know-*` trả mã 3.
- **Ed25519 (`ed1`):** hạ tầng có sẵn (`sign_approval_ed25519()`, khoá công ở `config/gate_ed25519_pubkeys/`). Phát khoá bằng nút `Phat Khoa Ed25519.command` — **CHỈ BÁC SĨ TỰ BẤM** (hướng dẫn `HUONG-DAN-PHAT-KHOA-ED25519.md`). Vai nào đã có khoá: `ls` thư mục khoá công. Chưa ép `ed1` bắt buộc (ép khi khoá riêng còn trên Mac sẽ làm hỏng con dấu niêm phong). HMAC là đối xứng: không chứng minh được người ký độc lập với chủ nhiệm. Rào `cryptography` bắt `BaseException` nhưng luôn ném lại `KeyboardInterrupt`/`SystemExit`.

## 5. Stack & nguyên tắc bắt buộc
- Python ≥ **3.11** (sàn đã khai; CI chạy 3.11 + 3.12): KHÔNG dùng cú pháp PEP 701 (lồng nháy cùng loại hoặc `\` trong biểu thức f-string); `ruff` bật `FA102`. Venv `~/.ebm-venv` ngoài OneDrive; `requirements.lock.txt`.
- Secrets: `~/.ebm-secrets/medical-ebm-automation.env` — `app/config.py` đọc nó TRƯỚC `.env` trong repo (không dùng symlink qua OneDrive: Windows nhận thành tệp văn bản ⇒ rơi về dữ liệu MOCK). Thứ tự: biến môi trường OS → kho secrets → `.env` repo. Không hardcode, không commit, không in khoá.
- Nguồn miễn phí không khoá: PubMed E-utilities, Europe PMC, Crossref, OpenAlex, openFDA… Nguồn có khoá (mặc định TẮT, bật khi có khoá thật): Semantic Scholar, Scopus, SerpApi Google Scholar (tính phí, có trần; chỉ là nguồn khám phá, tầng dự phòng sau Consensus; mọi bản ghi phải qua xác minh Crossref/PubMed). DynaMed đã gỡ (cần ID tổ chức từ EBSCO). Email: SMTP (Gmail App Password). Sổ nguồn và trạng thái: `tools/sources_health.py`.
- **VPN BẬT là mặc định:** NCBI chạy được, Scopus trả 403 — đánh đổi đã chấp nhận, không phải lỗi. NCBI chặn IP (chuyển hướng `misuse.ncbi.nlm.nih.gov`) ⇒ `HttpClient` báo lỗi ngay, không retry; gỡ chặn chỉ bằng liên hệ `info@ncbi.nlm.nih.gov` hoặc chờ.

**Nguyên tắc bắt buộc:**
1. API keys CHỈ trong `.env`/kho secrets, không hardcode; `.gitignore` loại trừ `.env`.
2. Docstring và comment bằng tiếng Việt, code rõ ràng cho người mới học.
3. Mọi hàm gọi API có error handling + retry.
4. KHÔNG lưu thông tin định danh bệnh nhân (PII).
5. Mọi output y khoa kèm disclaimer «Cần bác sĩ kiểm chứng» và ghi nguồn (PMID/DOI).
6. Trả lời bác sĩ bằng TIẾNG VIỆT (§0.1).

## 6. Chứng cứ lâm sàng — dashboard, bộ năm và cổng liêm chính
### 6.1 Mẫu dashboard
- MẶC ĐỊNH mẫu **Evidence Workbench** (nền sáng, 3 cột, khối GRADE Evidence-to-Decision); **Dark Analyst** chỉ khi bác sĩ yêu cầu. Hai mẫu dùng CHUNG schema `DATA` (meta/summary/items[] + `etd`): `dashboard_mockups/templates/evidence-workbench-template.html` · `dark-analyst-template.html` · đặc tả `dashboard_mockups/templates/DESIGN-SPEC.md`. Dashboard mới = copy template, chỉ thay khối `DATA`.
- Đổi bố cục cho TẤT CẢ = sửa template → đồng bộ y hệt sang `EBM_MASTER/skill_assets/web-dashboard-*.html` → `python3 EBM-Dashboards/tools/reskin_dashboards.py` (giữ `DATA`, backup `_reskin_backup/`). KHÔNG sửa tay `EBM_WEBAPP.html`/`DANH_MUC.html` (sinh lại mỗi lần sync).
- `DATA.summary` chỉ có ĐÚNG 4 khoá: `conclusion` · `doNow` · `dontDo` · `redFlags` (khoá lạ bị vứt âm thầm trên mọi sản phẩm — cổng chặn cứng). Thêm trường mới phải nối dây ở template + 3 bộ sinh phái sinh.

### 6.2 Dây chuyền MẶC ĐỊNH khi gọi skill `cap-nhat-chung-cu-y-khoa`
Dựng dashboard (EW) → cổng liêm chính → an toàn thuốc (nếu liên quan) → `build_library.py add` → phái sinh → **BỘ NĂM** bằng MỘT lệnh: `python3 tools/xuat_goi_cap_nhat.py <dashboard>.html --online` → ① dashboard · ② bản đọc `derivatives/<mã>_ban-doc.html` (cờ đỏ và việc cần làm đứng trước) · ③ Word `derivatives/<mã>_TaiLieuChiTiet.docx` · ④ Word dạng HTML · ⑤ PDF giữ màu (Chrome/Edge headless; thiếu trình duyệt thì bỏ qua, không đổi mã thoát) · ⑥ làm mới `cloud-mirror/`. `--online` chạy `verify_dashboard.py --online --strict-sources`; chỉ khi PASS bản Word mới nhận `--verified`; lỗi an toàn ⇒ **mã thoát 3, chặn xuất**. Không có pandoc thì bước ④ dùng `tools/docx_sang_html_khong_pandoc.py` (python-docx, giữ màu ô). Xong thì mở cả bộ cho bác sĩ ngay trong Claude. KHÔNG tự chạy `EBM_MASTER/tools/sync_all.py`/Antifacts (chỉ khi bác sĩ yêu cầu). Dashboard xuất vào `EBM-Dashboards/`.
- Word của dashboard: `EBM-Dashboards/tools/build_dashboard_docx.py` (không tự viết script mới; `--parts <json>` để nhóm phần). Markdown → Word Times New Roman: `tools/md_sang_docx_times.py` (không commit `.docx` sinh ra). Tự ghép OOXML: `tcBorders` trước `shd`, `pBdr` trước `spacing`; xác thực bằng `scripts/office/validate.py` của skill `docx` (máy Mac không có LibreOffice).
- Kênh thứ 3 — bản tin trong khung chat — theo `dashboard_mockups/templates/CHAT-BRIEF-SPEC.md`: rút gọn được, thêm khẳng định mới thì không; giữ mệnh đề điều kiện («ngoài thai kỳ», «nếu không chống chỉ định»); số liệu gắn đúng kết cục nó đo.

### 6.3 Luật của cổng `verify_dashboard.py` (4 bản đồng bộ byte — sửa nguồn chuẩn rồi đồng bộ)
- **Liêm chính:** số liệu trích ĐÚNG nguồn; RoB 2 chỉ cho RCT; kèm PMID/DOI; KHÔNG PII.
- **KHÔNG BAO GIỜ nâng `gradeLevel`** để qua cổng. `gradeLevel:'na'` = nguồn không dùng thang GRADE, KHÔNG phải «chứng cứ yếu». `apply` trên `na`/`low` hoặc chỉ dựa `Consensus` ⇒ chặn, trừ khi khai `normativeBasis` ∈ {`contraindication`, `drug-label`, `official-classification`, `guideline-strong-rec`, `guideline-explicit-criteria`} và đủ BA điều kiện: `design` thật là Guideline/Nhãn thuốc (**Consensus không bao giờ đủ**) · `normativeBasis` hợp lệ · `gradeSource` có phân hạng nguyên bản. Nguồn ĐÃ tự phân hạng `low`/`vlow` thì vẫn chặn. Hạ một chống chỉ định/nhãn FDA xuống «cân nhắc» là GIẢM an toàn.
- `gradeBy` (ai chấm mức này) — cảnh báo khi thiếu (`python3 tools/kiem_phan_hang.py`). `gradeSource` là lời NGƯỜI SOẠN khai — tra nguồn trước khi dựa vào nó để đổi phân hạng. `design` là chuỗi tự do — không dùng làm căn cứ quyết định an toàn.
- `provenanceUnknown: true` + `provenanceUnknownLyDo` ⇒ một cảnh báo (gói không tái lập được); khai mà thiếu lý do ⇒ lỗi cứng; miễn trừ CHỈ bỏ phần hợp đồng nguồn, mọi luật cấp item vẫn chạy. Gói MỚI phải khai đủ chiến lược tìm theo PRISMA-S. KHÔNG bịa hợp đồng nguồn cho một lần tìm đã qua.
- **Rút bài:** nguồn đã rút ⇒ chặn cứng + dải đỏ trên bản đọc. «Rút và thay» (retract-and-replace): đối chiếu số liệu với BẢN ĐÃ THAY rồi trích đúng bản đó — không đổi sang định danh khác của chính bài đã rút, không xoá mục. Thông báo rút là **bản đính chính bị rút** ⇒ vẫn chặn, thông điệp «CẦN BÁC SĨ XEM»; `python3 tools/mau_ky_rut_bai.py` dựng mẫu `EBM-Dashboards/rut-bai-da-xem-xet.cho-ky.json`; chỉ bác sĩ ký vào `rut-bai-da-xem-xet.json` (gắn vân tay tập thông báo). Agent KHÔNG ký.
- Hai bản cùng chủ đề nói ngược nhau ⇒ dải cam trên bản đọc; chỉ ĐẶT CẠNH, không đoán bên nào đúng, không đổi `decision`.
- Cổng `return` sớm hoặc parser sai (nháy lồng) từng che lỗi an toàn thật — sửa cổng xong phải kiểm cổng CHẠY TỚI các luật phía sau.

### 6.4 Độ mới & độ tin cậy chứng cứ
- **Một lệnh:** `python3 tools/chu_trinh_chung_cu.py [--nhanh]` — ① nguồn có THẬT (dừng nếu không) → ② độ tươi → ③ xác minh từng nguồn → ④ rút bài → ⑤ mâu thuẫn → ⑥ dây chuyền. Chỉ ĐO và BÁO, không tự áp dụng.
- `python3 tools/kiem_nguon_that.py [--nhanh]`: 🔴 chỉ cho cấu hình (MOCK bật / thiếu `NCBI_EMAIL` = dữ liệu SAI), 🟡 cho mạng.
- Sổ xác minh: `python3 tools/so_xac_minh_nguon.py [--quet …] --vong 3` — tích luỹ bằng chứng theo TỪNG định danh, chỉ ghi THÀNH CÔNG; hạn 180 ngày cho tồn tại/metadata, 30 ngày cho trạng thái rút bài.
- **Rút bài = CHUỖI 3 TẦNG** (`app/sources/retraction_chain.py`): Retraction Watch ngoại tuyến (`tools/tai_retraction_watch.py`, làm mới 30 ngày) → NCBI → Europe PMC; DOI thêm tầng Crossref `updated-by`. **Gộp bất đối xứng, đừng đảo:** tín hiệu DƯƠNG từ bất kỳ nguồn nào là nhận; «ok» chỉ từ nguồn đã THỰC SỰ lấy được bản ghi; Retraction Watch không bao giờ nói «ok»; không kết luận được ⇒ KHÔNG BIẾT. `correction`/`erratum` không tính là rút bài. Định danh mang bảo đảm nào phải chịu đúng phép kiểm của bảo đảm đó, dù ghi bằng PMID, DOI hay URL.
- Agent PHẢI TRA rút bài bằng công cụ, không trả lời bằng trí nhớ; không tra được ⇒ ghi «chưa kiểm rút bài», TUYỆT ĐỐI không ghi «chưa bị rút». Bài quá mới thường chưa có publication type — đừng loại.
- Độ tươi: `python3 tools/kiem_do_tuoi_chung_cu.py` (báo trung vị + chủ đề lâu nhất; ngưỡng báo động 120 ngày). Chứng cứ bị vượt qua: `python3 tools/kiem_chung_cu_vuot_qua.py` (danh sách ĐÁNG ĐỌC, không phải «đã sai»; chỉ tin manifest ĐỦ). Đặt cạnh: `tools/dat_canh_chung_cu_moi.py` (không phán chiều). Con số: `python3 tools/kiem_so_lieu.py` — ba mức ✓ / 🟠 / ⚪, cố ý KHÔNG có mức «SAI».
- Đăng ký chủ đề: `python3 tools/dang_ky_chu_de.py` — PHIÊN BẢN NỐI TIẾP (cùng lát cắt, bản mới thay bản cũ) khác LÁT CẮT KHÁC NHAU (bổ sung, đọc cả nhóm); khoá nhóm là đường dẫn, không phải ngày. Sửa một dashboard KHÔNG tự lan sang bản khác cùng chủ đề — sửa xong chạy công cụ này. Mâu thuẫn đã duyệt: `EBM-Dashboards/mau-thuan-da-duyet.json`; quyết định đã duyệt: `EBM-Dashboards/quyet-dinh-da-duyet.json` (CHỈ bác sĩ ghi) + `tools/kiem_quyet_dinh_da_duyet.py`.
- **Khâu THU NHẬN** (`surveillance_scan.py`, 3 bản — sửa nguồn chuẩn `sync/skills/cap-nhat-chung-cu-y-khoa/tools/` rồi `python3 tools/dong_bo_scanner_giam_sat.py`): tìm theo tầng (guideline → tổng quan/gộp → RCT lớn) + tầng `moi_vao_pubmed` theo `edat` KHÔNG lọc publication type; 4 nguồn thẩm quyền Cochrane/NICE/USPSTF/WHO; làn preprint (nhãn CHƯA BÌNH DUYỆT), ClinicalTrials.gov, Scopus. Mỗi ứng viên mang `rut_bai`, `pubtype` thật, `da_co_trong_kho`, `chua_binh_duyet`. NCBI chặn ⇒ dự phòng Europe PMC dịch thẻ fail-closed, trạng thái `PASS_DEGRADED`, con trỏ KHÔNG tiến. «0 ứng viên» ≠ «không có chứng cứ mới». Loại xuất bản consensus hiện hành của PubMed là `"consensus statement"[pt]` (NLM đã bỏ `consensus development conference`); thẻ `[pt]` bị báo không nhận ⇒ kiểm `query_translation` qua connector PubMed trước khi kết luận.
- **Chuẩn quốc tế agent phải cầm** (bài gốc đã tra PubMed): AGREE II (PMID 20656455 — ở agent TIÊU THỤ guideline; guideline không mô tả Miền 3 Rigour thì ghi rõ cạnh khuyến cáo) · RIGHT (PMID 27893062; bản 2017 hiện hành, đang được cập nhật) · PRISMA-S (PMID 34285662) · ROBIS (PMID 26092286) · GRADE-CERQual (PMID 26506244).
- **Thêm một luật ở CỔNG thì phải DẠY AGENT cùng lúc** (doctrine phải nhắc mọi trường cổng bắt buộc). Một công cụ KHÔNG ai gọi thì với dây chuyền hằng ngày nó không tồn tại — xếp việc vào loại «tuỳ chọn» là cách nó lặng lẽ không bao giờ chạy.
- Canary đầu–cuối: `python3 tools/thu_dau_cuoi_chung_cu.py` (ngoại tuyến, dữ liệu giả; chứng minh cổng BẮT được lỗi nếu gói đi qua cổng — không chứng minh agent đã GỌI cổng).
- Giám sát định kỳ fail-closed: owner thu thập DUY NHẤT là `medical-ebm-automation/scripts/weekly_safety.sh` + `monthly_update.sh`; routine khác chỉ dùng hàng chờ candidate. Chỉ `READY_FOR_CONTROLLED_DEPLOYMENT` từ `python3 medical-ebm-automation/tools/verify_evidence_surveillance_deployment.py --online` mới cho phép candidate-only; Claude Code không tự điền PASS hay ký UAT. Chạy bộ chốt/contract-check ghi đè `medical-ebm-automation/reports/EVIDENCE_SURVEILLANCE_DEPLOYMENT_REPORT.*` bằng bản ngoại tuyến — `git checkout --` hai tệp đó trước khi commit.
- CHƯA có kiểm chéo ngữ nghĩa độc lập cho mục `apply` (đúng quần thể, đúng con số, mệnh đề điều kiện): các cổng hiện kiểm hình thức và «nguồn có thật», không kiểm nội dung đúng.

## 7. Tầng cuộc gặp (phòng khám)
- `python3 tools/tra_diem_kham.py "<câu hỏi>"` — chỉ trả thẻ đã duyệt; thiên về ĐỘ CHÍNH XÁC (nhiều câu có thẻ vẫn ra «chưa giám sát»); không giải được đồng âm/đa nghĩa tiếng Việt và ngưỡng số (vd EF<35%) — LUÔN đọc TIÊU ĐỀ thẻ trước khi dựa vào. Bộ vàng `quality/eval/tra-diem-kham/bo-vang.json` do máy soạn, CHỜ bác sĩ duyệt Y/N (`bac_si_duyet`) — trước đó «đạt» chỉ là máy tự chấm. `--demo` không ghi nhật ký; thẻ `from_engine` bị loại khỏi điểm khám.
- `python3 tools/so_viec_chua_dong.py` — sổ việc còn treo sau khi bệnh nhân về (xét nghiệm, chuyển tuyến, tái khám…): `--them` (bắt buộc hạn) · `--dong` · `--huy --ly-do` · `--ds` · `--tuan`. Chỉ ĐO và NHẮC, không tự đóng việc; chặn PII mẫu độ chính xác cao; sổ `state/` nằm ngoài git.
- `python3 tools/kiem_safety_net.py` — kiểm CẤU TRÚC ngân hàng cờ đỏ/lời dặn (`clinical_runtime/safety_net_templates.json`, 9 luật; R9 chặn cứng khi cờ `enforce_safety_net_templates` bật mà không hội chứng nào có nguồn). Hai trục TÁCH RIÊNG: cờ đỏ cho bác sĩ ≠ lời dặn bệnh nhân. Lời dặn do bác sĩ viết — máy KHÔNG sinh thay.

## 8. Hệ nghiên cứu G0–G10 (`medical-ebm-automation/`)
- Mỗi đề tài có thư mục riêng `exports/<study>/` dùng xuyên suốt. Cổng cứng và vai: nguồn sự thật DUY NHẤT `tools/gate_contract.py::_GATE_REQUIRED_STAKEHOLDERS` (doctrine đếm khớp: `tools/verify_hard_gate_count_consistency.py`). Bác sĩ điền xác nhận ở `exports/<study>/study_meta.json → gate_params.Gx`, KHÔNG ở tệp `.md` (bị ghi đè khi chạy lại).
- **Cả 11 cổng có `tools/gN_quality_gate.py`;** `approve_gate.py` chấm quality gate của cổng TRƯỚC khi ghi ledger. Tên artifact là HỢP ĐỒNG — không đổi (vd `G8_A9_PRESUBMISSION_<study>.md` bị băm trong chữ ký; tên «A9» lệch crosswalk là lệch ĐÃ BIẾT).
- G0 (`BLOCKED` · `DRAFT_READY_NEEDS_HUMAN_REVIEW` · `PASS_G0_CONFIRMED`): tra ClinicalTrials.gov tự động; R5 (chặn chỉ thị lâm sàng lồng trong câu hỏi) đã hẹp phạm vi — câu mệnh lệnh diễn đạt lại có thể lọt (đánh đổi có chủ ý).
- G3: KHÔNG phải cổng ký nhưng CHẶN CỨNG (quality BLOCKED hoặc lớp chấm crash ⇒ mã thoát 3). Thiết kế mô tả tính theo độ chính xác: `--prevalence` + `--precision` (không nhét p vào `--effect-size`). Effect size phải có nguồn (PMID/DOI/MCID) — không bịa.
- G4: SAP được KÝ và KHOÁ; `G4-AUTO-03` đọc §12 so với `G3_checkpoint.json` SỐNG; SAP ghi N HIỆU LỰC (`confirmed_n`) cho mọi thiết kế; SAP vắng sạch mục bắt buộc bị chặn. RCT có thêm §13–§15 (giữa kỳ & quy tắc dừng · DMC · tổn hại/ngừng/tuân thủ) nối SAU §12 — không đánh số lại §1–§12.
- G5: `G5-AUTO-03` đòi nội dung thật dưới từng nhãn DMP (không chỉ có nhãn).
- G8: nhãn đạt là `PASS_G8_REVIEW_RECORDED` (KHÔNG mang chữ «ĐỘC LẬP» vì HMAC đối xứng); đòi `G8_PEER_REVIEW_REPORT_<study>.md` do người phản biện THẬT viết (máy không sinh); kiểm ICMJE 1/2026 Mục V.A/V.B, III.L, IV.B; đối chiếu `reviewer_ref` giữa các cổng.
- G9: `READY_FOR_G9_PI_APPROVAL` đòi đủ nhóm tiêu chí người thật (ICMJE 4 tiêu chí, CRediT, COI, khai AI, Data Availability, xác nhận thể chế…).
- Biên nhận guardrail lâm sàng (`clinical_checkpoint`) có ba mức: không dẫn (tự khai của mô hình) · dẫn id CÓ THẬT trong `observability/APPRAISALS.jsonl` · dẫn id KHÔNG CÓ ⇒ vi phạm. Cố ý chưa bắt buộc biên nhận.
- **Annex 2 (ICH E6(R3)):** `tools/annex2_quality_gate.py` dùng ở G1/G2; tiêu chí là chuẩn QUY PHẠM — agent KHÔNG tự bịa để «cho có cổng».
- Khuôn đề cương 18 mục / 24 thành phần lõi; đánh số qua khoá bền `skill_standards.DE_CUONG_SECTION_KEYS` (`de_cuong_heading()`), không viết cứng số mục. Checklist SPIRIT 2025 và PRISMA-P 2015 nguyên văn tiếng Anh trong `tools/protocol_checklist_items.py` (sinh tự động — không gõ tay, không dịch); G10 không tự tick. RCT có §6.2–§6.5 (TIDieR, ngẫu nhiên hoá/làm mù, lịch trình, PPI).
- Lệnh: `python3 tools/study_readiness.py --study <mã>` (việc CHƯA làm, quyết định treo, chỗ trống; cố ý bi quan, không in «sẵn sàng») · `python3 tools/list_studies.py` · `python3 tools/kiem_chi_tiet_he_nghien_cuu.py --study <mã>` (11 cổng × 5 trục; mã 0/1/2/3) · `python3 tools/verify_exports_integrity.py` (pre-commit; bắt placeholder bị sửa nhầm) · `python3 tools/g<N>_quality_gate.py --study <mã>`.
- Sửa văn bản hàng loạt phải bảo vệ placeholder (dấu `—` vừa là dấu ngắt vừa là ô trống bảng); đối chiếu số lượng với bản git trước khi commit. Fixture không thay được một đề tài thật đi hết dây chuyền. Xuất Word mặc định vào `exports/<đề tài>/` sau mỗi cấu phần nghiên cứu.
- Đề tài thật duy nhất hiện nay: `hai-long-benh-nhan-C1a-BVQY175` — tiến độ đo bằng `study_readiness.py`.

## 9. Tự động & lịch nền
- Tác vụ lịch (app Claude trên Mac): đo sống bằng `list_scheduled_tasks` + `list_task_runs` và `python3 tools/kiem_lich_nen.py` (cảm biến theo TỪNG kỳ; kỳ vọng ở `sync/lich-nen-ky-vong.json`). Khôi phục/sửa tác vụ: chép `SKILL.md` từ NGUỒN GIT `sync/scheduled-tasks/` sang runtime, không chép ngược. Không tạo tệp hàng chờ tuần «gộp» để hết đỏ. App từng xoá cả loạt tác vụ — một phép đo lịch là ảnh chụp.
- Lưới đỡ khi máy không thức đúng giờ: `tools/tu_khoi_dong.py --phong` (chỉ phóng script chủ sở hữu trong allowlist; kết quả vào hàng ứng viên; tắt bằng `--tat`). Tự vá máy móc: `tools/tu_sua_chua.py --ap-dung` (KHÔNG đụng nội dung y khoa).
- Bộ chốt bài học: `python3 tools/chot_hoi_quy_bai_hoc.py` (mỗi mục là một lỗi CÓ THẬT, kiểm HÀNH VI trên mã sống, nhanh, ngoại tuyến). Thêm/sửa mục ⇒ chạy TRỌN BỘ; miễn trừ theo từng match (`bh10-mien:` kèm lý do). Lịch sử từng mục: `audit/NHAT-KY-SU-CO.md`.
- CI GitHub Actions hai repo: đo sống bằng `gh run list`; bật/tắt Actions và hạn mức là việc của bác sĩ.
- **«Hệ còn gì để làm?»** = `python3 tools/tu_de_xuat_viec.py` (👤 thẩm quyền bác sĩ · 🤖 máy chạy được · 🛎 máy làm được nhưng chưa ai chạy). Báo cáo đánh giá: thư mục `audit/` (12 = đánh giá hoàn thiện 21/09; 15 = nguồn chứng cứ trên Cloud 24/09).

## 10. Lệnh thường dùng
> Chạy trong `medical-ebm-automation/` (venv `~/.ebm-venv`) trừ khi có tiền tố `../`.
- Cài: `pip install -r requirements.txt` · chạy app: `python run.py` · kiểm nhanh: `python -m compileall -q app scripts tests` · test: `pytest` · lint: `ruff check`.
- Kiểm + đồng bộ toàn hệ: `python3 ../tools/upgrade_verify.py` (enforce→sync→check→routing→assess→audit→orchestrator).
- Căn chỉnh repo/Claude Code/Codex: `python3 ../tools/verify_claude_code_repo_alignment.py` · rubric ↔ LESSONS: `python3 ../tools/verify_lessons_rubric_alignment.py`.
- Pipeline chứng cứ (fixture offline, không PII): `python3 ../tools/verify_clinical_evidence_update_pipeline.py`.
- Chu trình tự động có kiểm soát: `python3 ../tools/run_controlled_automation_cycle.py` · control plane: `python3 ../tools/run_orchestrator.py "<ca/đề tài/câu hỏi>"` (dry-run mặc định; `--execute` chạy Codex trong phiên tạm chỉ-đọc; không thay pipeline nghiên cứu canonical hay cổng bác sĩ/IRB/PI).
- Kiểm rút bài + độ tươi 32 thang điểm (QUÝ, tác vụ `kiem-thang-diem-quy`): `python3 tools/kiem_do_tuoi_thang_diem.py [--nhanh]` — không bao giờ tự đổi cut-off.
- Trạng thái tổng: `python3 ../tools/tu_de_xuat_viec.py` · độ tươi: `python3 ../tools/kiem_do_tuoi_chung_cu.py` · an toàn đồng bộ: `python3 ../tools/sync_safety_check.py`.

_Nguyên mẫu cũ `ebm-copilot/`: `pip install -r requirements.txt` → `python -m src.research.digest` → `pytest tests/` (chỉ tham chiếu)._
