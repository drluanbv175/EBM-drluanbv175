# SỔ BÀN GIAO ĐỒNG BỘ — 28/08/2026 (phiên cloud)

> Soạn từ phiên Claude Code chạy trên đám mây: **không có OneDrive, không thấy hai máy**.
> Phiên này chỉ chạm được tầng **GITHUB** — và đã đưa tầng đó về MỘT nguồn duy nhất.
> Tầng **ONEDRIVE** (EBM-Dashboards/ · EBM_MASTER/ · memory) chỉ hai máy tự kiểm được — xem mục C.

## A. Đã làm trên GitHub hôm nay

1. **`medical-ebm-automation`** — `master` chậm 51 commit (dừng 15/08) so với nhánh làm việc
   `feat/r1-1-2-design-gap-remediation` (27/08). Đã hợp nhất (0 xung đột, CI xanh run 387) rồi
   **fast-forward nhánh làm việc lên cùng commit `96192a3`** — hai tên nhánh nay trỏ MỘT chỗ,
   máy đứng nhánh nào `git pull` cũng nhận cùng trạng thái. Diff cây của cú FF = RỖNG.
2. **Repo gốc** — gộp 3 nhánh phiên cloud tồn đọng vào `master` (`834393f`):
   - `claude/chan-doan-dieu-tri-viem-da-khop-6bocoa` (27/08): gói lâm sàng viêm đa khớp,
     thư mục mới `lam-sang/`, dừng Cổng A, 0 PII, 14 nguồn PMID/DOI.
   - `claude/neuropsychiatric-…-k5qvss` (24/08): template thêm nhóm «thai kỳ» + «trẻ em·vị
     thành niên» (cả 2 cặp bản sao), `docx_sang_html_khong_pandoc` giữ cả MÀU CHỮ.
   - `claude/outpatient-clinical-practice-sm8r27` (22/08): sổ việc chưa đóng + chốt
     safety-netting (2 công cụ + 2 bộ test), nối 3 agent, md→docx Times, 2 bài audit, và
     **vá 6 chỗ cú pháp PEP 701** — master trước merge này KHÔNG biên dịch được trên Python
     3.11 (đúng sàn doctrine khai) ở 5 file tools/; không máy nào thấy vì Windows 3.12/Mac 3.14.
3. **Đánh số lại bài học (đúng tiền lệ BH68):** nhánh outpatient rẽ lúc sổ mới tới BH69 nên tự
   đặt BH70/71/72 — trùng số ba mục master đặt song song. Ba chốt của nhánh nay là
   **BH78 · BH79 · BH80** (nội dung giữ nguyên; audit/03 và CLAUDE.md đã sửa theo).
   Sổ đăng ký sau hoà: **BH01–BH80, 0 trùng**.
4. **Kiểm sau gộp** (trong container, bố cục lồng tái hiện bằng symlink):
   enforce_agent_guardrails 50/50 (0 phải sửa) · sync_agents_to_codex --check PASS ·
   check_claude_codex_sync_health PASS 50/50 · verify_plugin_orchestration PASS ·
   compileall tools+ops sạch **dưới 3.11** · chốt BH chạy ĐỐI CHỨNG trước/sau merge:
   **0 mục đỏ mới, 4 mục đang đỏ trên master được chữa lành** (BH05·19·20·32 — nhóm gọi vào
   các file lỗi 3.11). 32 mục đỏ còn lại đỏ ở CẢ HAI phía = thiếu môi trường container
   (không OneDrive/plugin/secrets), trên máy thật vốn xanh.

## B. Mỗi máy chạy MỘT lượt (thứ tự bắt buộc)

Mở terminal ở thư mục `Claude AI` (Mac: `~/OneDrive/Claude AI` · Windows: `C:\Users\Admin\OneDrive\Claude AI`), đợi OneDrive XANH:

```bash
python3 tools/sync_safety_check.py            # ① 🔴 thì DỪNG, xử lý xong mới bước ②
git pull                                       # ② repo gốc (master)
git -C medical-ebm-automation pull             # ③ đứng nhánh nào cũng nhận cùng trạng thái
python3 tools/dong_bo_tat_ca.py --ap-dung      # ④ 8 làn: an toàn→git→skill→agent→plugin→hook→bộ nhớ→kho
python3 tools/sync_memory.py                   # ⑤ bộ nhớ dự án (ngoài OneDrive, phải chạy tay)
```

(Windows dùng `python` nếu `python3` không có trong PATH.)

Sau ①–⑤, chốt SessionStart tự chạy phần còn lại ở phiên Claude kế tiếp. Chốt BH kỳ vọng
**BH01–BH80 xanh**; nếu máy nào đỏ đúng BH78/79/80 thì đọc docstring trong
`tools/chot_hoi_quy_bai_hoc.py` — đó là 3 chốt mới về sổ việc treo · safety-netting · sàn 3.11.

## C. Chỉ máy thật kiểm được (phiên cloud KHÔNG làm hộ — đừng tin ai nói ngược lại)

- OneDrive xanh 2 chiều; `EBM-Dashboards/` + `EBM_MASTER/` là DỮ LIỆU ngoài git.
- venv `~/.ebm-venv` · secrets `~/.ebm-secrets` · `~/.claude/settings.json`
  (giữ `skillListingBudgetFraction: 0.08` — bản vá 12/08, đè cả file là mất).
- Windows: binary harness v5.13.1 **chưa chạy thử** (ghi chú 26/08) — thấy chậm/lỗi lạ ở
  Write/Edit/Bash thì nghi hook harness trước tiên.

## D. Chờ QUYẾT ĐỊNH bác sĩ (không máy nào tự làm)

1. `sync/plugin-manifest.json`: mọi mục còn `da_xac_nhan: false` ⇒ chốt trôi dạt plugin mới
   chỉ CẢNH BÁO. Xác nhận `can_o_may`/`ly_do` từng mục thì nó thành cổng thật.
2. **7 nhánh tồn tháng 7** trên repo gốc (75–193 commit riêng, đã bị đợt «hợp nhất lịch sử
   535 commit» 15/08 vượt qua) + `release/clinical-runtime-unlock-2026-07-25`: giữ làm chứng
   tích hay xoá — phiên cloud KHÔNG đụng.
3. `medical-ebm-automation` nhánh `main` đóng băng ở V4.3.4 (28/06, có tag frozen) — để nguyên
   như lineage lưu trữ; default branch trên GitHub là `feat/r1-1-2-design-gap-remediation`
   (nay trùng master nên vô hại). Muốn đổi default sang `master` thì làm trên web GitHub.

— Soạn tự động từ phiên cloud 28/08/2026 · Cần bác sĩ kiểm chứng.
