# CẢNH BÁO KHẨN — 2026-08-17

- 🔴 CỔNG KIỂM RÚT BÀI TRẢ SAI KHI CHẠY BẰNG `python3` HỆ THỐNG — đo được hôm nay, cùng 7 PMID, chỉ khác trình thông dịch. `python3 medical-ebm-automation/tools/check_citation_retraction.py` in `[retraction_chain] tầng NCBI vắng mặt (thiếu thư viện — python3 hệ thống?)` rồi trả **"⚠️ KHÔNG KIỂM ĐƯỢC" cho 7/7 PMID**; chạy lại đúng cùng lệnh bằng `~/.ebm-venv/bin/python` thì **7/7 = ✅ OK (nguồn kết luận: pubmed)**. Nghĩa là tầng NCBI rụng ÂM THẦM, chỉ còn nền ngoại tuyến Retraction Watch — mà nền đó theo luật bất đối xứng **vĩnh viễn không được nói "ok"** ⇒ mọi PMID hoá "chưa kiểm". Đây là biến CHƯA BIẾT thành CÓ VẤN ĐỀ (họ lỗi BH08), và ở hướng ngược lại nó chặn oan cổng A12. **Việc cần làm: luôn gọi công cụ chuỗi rút bài bằng `~/.ebm-venv/bin/python`, và cân nhắc cho `retraction_chain` thoát mã khác 0 kèm thông điệp rõ khi tầng NCBI vắng thư viện thay vì chỉ `logger.info`.** Đã dùng venv cho toàn bộ 9 PMID của gói tuần W34.
