# CẢNH BÁO KHẨN — 2026-09-01

- 🔴 **BỘ QUÉT GIÁM SÁT BÁO `PASS` TRONG KHI ĐƯỜNG TÌM CHÍNH ĐÃ CHẾT — 4/4 làn nguồn thẩm quyền trả 0 một cách GIẢ.**
  Đo hôm nay trong lượt quét gói tuần W36: NCBI E-utilities trả trang **`WWW Error Blocked Diagnostic`**
  (HTTP **200** kèm HTML, không phải lỗi mạng — nên mã gọi thấy như "thành công"). Hệ quả dây chuyền:
  **(a)** `99/99` ứng viên y văn vào bằng `Europe PMC fallback for PMID`, **0** mục qua NCBI;
  **(b)** chỉ tầng 4 `moi_vao_pubmed` chạy — **tầng 1 guideline/đồng thuận, tầng 2 tổng quan/gộp,
  tầng 3 RCT lớn trả 0 trên TOÀN BỘ 47 chủ đề**; **(c)** cả **Cochrane · NICE · USPSTF · WHO** trả 0.
  Trong khi đó Cochrane THẬT có **84 bài** trong cửa sổ 90 ngày (`JOURNAL:"Cochrane Database Syst Rev"`
  + khoảng ngày), còn **64** sau khi áp đúng bộ lọc PUB_TYPE của chính đường dự phòng.
  **Bộ quét vẫn tự khai `status=PASS`, `successful_topics=47/47`, `failed_topics=0`.**
  **Nguyên nhân gốc, đã cô lập bằng thực nghiệm từng mệnh đề:** `search()` chỉ lùi sang Europe PMC khi
  bắt `RuntimeError`, và **truyền nguyên truy vấn cú pháp PubMed sang Europe PMC**. Chạy tay đúng chuỗi
  bộ quét gửi: `"Cochrane Database Syst Rev"[ta]` → **1.148 hit**; thêm `AND FIRST_PDATE:[…]` → **119**;
  thêm `AND (SRC:MED OR HAS_FT:Y)` → **0**. Truy vấn NICE y hệt như bộ quét gửi → **0**.
  Đường dự phòng còn **bỏ qua con trỏ tăng dần** (dùng `days=90` thay `mindate/maxdate`) và **tự áp bộ
  lọc PUB_TYPE riêng** — đúng thứ **BH38** cấm ở khâu tìm.
  **Vì sao nguy hiểm hơn một lần quét hỏng:** nhãn `tang` in ra báo cáo là **biến vòng lặp**, không phải
  thứ đã thật sự chạy — nên bốn tầng cùng gọi một truy vấn dự phòng mà báo cáo vẫn in tên tầng như
  thường. Người đọc nhận một gói **trông như đã quét đủ bốn tầng**. Cùng họ lỗi «con số không đo thứ nó
  tự nhận» (BH32) và «luật CÓ MẶT nhưng không bao giờ chạy tới» (BH01/BH27), lần này ở khâu **THU NHẬN**
  — thượng nguồn của mọi cổng phía sau, nên không cổng nào ở hạ nguồn bắt được.
  **Việc cần làm (mã nguồn `EBM-Dashboards/tools/surveillance_scan.py`, KHÔNG tự sửa trong phiên gói tuần):**
  (1) `search_europe_pmc()` phải **DỊCH** truy vấn PubMed sang cú pháp Europe PMC (`[ta]`→`JOURNAL:`,
  `[ti]`→`TITLE:`, `[pt]`→`PUB_TYPE:`), không truyền thô; (2) khi tầng NCBI câm, **`status` KHÔNG được
  là `PASS`** — phải có trạng thái riêng kiểu `PASS_DEGRADED` + đếm số làn thật sự chạy được;
  (3) nhãn `tang` phải ghi tầng ĐÃ CHẠY, không phải tầng dự định; (4) đường dự phòng phải tôn trọng
  con trỏ và **không** tự áp `PUB_TYPE` (BH38).
  **Trạng thái gói W36:** vẫn xuất 7 thẻ từ phần vớt được của tầng 4, và **mục ⓵ của queue nói rõ gói
  dựng trên mẻ ứng viên KHUYẾT**. Không hạ tiêu chuẩn, không làm đẹp số.

- 🔴 **CÙNG SỰ CỐ LÀM SẬP LUÔN KHÂU ĐỌC TOÀN VĂN — 7/7 thẻ tuần này kẹt ở `partial` vì HẠ TẦNG, không phải vì thiếu bản OA.**
  `gom_toan_van_dashboard.py --queue queue/tuan-2026-W36.md` dừng ở
  `🔴 HẠ TẦNG: elink không trả lời (JSONDecodeError)`; thêm `--unpaywall` **cũng không cứu được** vì bước
  `elink` (gọi NCBI) chặn TOÀN BỘ các tầng OA phía sau. Công cụ **từ chối kết luận độ phủ** thay vì báo
  «không có bản OA» — **đúng luật** (BH08: «không tra được» ≠ «không tồn tại»), nên đây là hành vi tốt,
  không phải lỗi thứ hai.
  **Nhưng hệ quả thì thật:** đã xác nhận độc lập qua Europe PMC rằng **2/7 thẻ CÓ bản truy cập mở** —
  PMID 42287020 (**PMC13263707**) và PMID 42139049 (**PMC13179568**). Hai bài đó lẽ ra đọc được toàn văn
  tuần này. Vì luật gói tuần chặn `partial` khỏi mức «Áp dụng ngay», **một sự cố hạ tầng đang trực tiếp
  hạ trần đề xuất của thẻ** — cần biết rõ để không đọc «0 thẻ Áp dụng ngay» thành «chứng cứ tuần này yếu».
  **Việc cần làm:** cho `gom_toan_van_dashboard.py` một đường vòng khi `elink` câm — Europe PMC đã trả
  `pmcid` ngay trong `resultType=core`, không cần NCBI để biết bài có nằm trong PMC hay không.

- 🟡 **Làn đối chiếu OpenAlex: 4 mục nguồn thẩm quyền đang đo SAI thứ chúng tự nhận.**
  `doi_chieu_openalex.py::_thuat_ngu()` bóc mọi thẻ trường PubMed rồi ném chuỗi còn lại vào `search=`
  của OpenAlex — nên `"Cochrane Database Syst Rev"[ta]` thành **tìm chữ tự do**, không phải lọc theo
  nguồn. Đo trên báo cáo hôm nay: **0/33** mục trong 4 mục Cochrane · NICE · USPSTF · WHO thật sự đến từ
  nguồn được nêu tên (mục "Cochrane" trả *Journal of Mental Health of Thailand*, *Women and Birth*,
  sâu răng ở Rwanda…). Không phải sự kiện lâm sàng khẩn ⇒ **ghi ở đây để có dấu vết**, và đã ghi trong
  gói tuần. Cách sửa cùng họ với mục trên: dịch sang `JOURNAL:` / `TITLE:` thay vì truyền thô.

- 🔴 **HỆ QUẢ NẶNG NHẤT, và là thứ DUY NHẤT tôi đã tự tay sửa: con trỏ quét ĐÃ TĂNG dù không quét được gì.**
  Sau lượt chạy, `.quet-cursor.json` ghi **2026-09-01 cho cả 49 mục** — kể cả 4 làn thẩm quyền vừa trả 0 vì
  truy vấn đã chết. Lượt sau bắt đầu từ `con_trỏ − 3 ngày` = **2026-08-29**, nên cửa sổ **21/08 → 29/08**
  đối với **tầng 1 guideline · tầng 2 tổng quan/gộp · tầng 3 RCT lớn** và **cả 4 làn Cochrane · NICE ·
  USPSTF · WHO** sẽ **KHÔNG BAO GIỜ được quét lại** — nó vừa bị đánh dấu «đã xong» trong khi thực tế
  chưa hề được nhìn tới. Đây là **mất chứng cứ IM LẶNG và VĨNH VIỄN**, tệ hơn bản thân lần quét hỏng:
  lần quét hỏng thì tuần sau chạy lại là xong, còn con trỏ đã nhảy thì không dấu vết nào cho biết đã mất gì.
  **Đã xử lý:** lùi **49/49 con trỏ về `2026-08-24`** — ngày lượt quét W35 chạy, mốc CUỐI CÙNG biết chắc
  đã quét đủ bốn tầng. Sao lưu nguyên trạng tại `.quet-cursor.json.bak-20260901-truoc-lui-con-tro`.
  **Vì sao tự làm việc này mà không chờ duyệt:** lùi con trỏ chỉ khiến hệ **quét NHIỀU hơn**, không bao giờ
  ít hơn; trùng lặp đã có `da_co_trong_kho` và ghi chú «đã trình» chặn (chính mã nguồn cũng ghi *«dedup phía
  sau chặn trùng nên lùi là rẻ»*). Nó **không** chạm vào `decision`, `gradeLevel`, thẻ, hay bất kỳ cổng nào —
  thuần sổ sách giám sát. Để nguyên thì mất chứng cứ chắc chắn; lùi thì chi phí tệ nhất là đọc lại vài mục
  đã thấy. Muốn hoàn tác: chép lại file `.bak` ở trên.

- 🟡 **Làn ĐẶT-CẠNH chứng cứ (2 tuần/lần, đến lịch tuần này) KHÔNG chạy được — cùng một gốc.**
  `dat_canh_chung_cu_moi.py` dựa vào `elink pubmed_pubmed_reviews` của NCBI. Theo dõi 5 phút: đứng ở
  **25/157 PMID**, **0,01 giây CPU** ⇒ kẹt chờ mạng. **Đã dừng có chủ ý, không ghi file.** Nếu để chạy tiếp,
  đầu ra sẽ là báo cáo *«không có nguồn tổng hợp mới hơn»* cho phần lớn 157 mục `apply` — **trấn an SAI sinh
  từ tra cứu đã chết**, tức tự tay tạo thêm một ca của chính lỗi đang được ghi ở hai mục trên. Bản
  `DAT-CANH-CHUNG-CU-MOI_2026-08-27.md` (114 mục) vẫn dùng được; chạy lại khi NCBI trả lời.

> Cần bác sĩ kiểm chứng. Không mục nào ở đây thay đổi thẻ hay quyết định lâm sàng — Cổng A/B thuộc bác sĩ.
