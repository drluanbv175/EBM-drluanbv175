# tools/rag — phụ thuộc cho CHẾ ĐỘ ĐẦY ĐỦ (embedder ngữ nghĩa + vector store local).
# Demo fallback (offline) CHỈ cần numpy — đã đủ để chứng minh luồng.
#
# Cài chế độ đầy đủ (local, miễn phí):
#   pip install -r requirements.txt --break-system-packages
#
numpy>=1.24            # bắt buộc (fallback dùng numpy)
sentence-transformers>=2.2   # embedder ngữ nghĩa (tải model all-MiniLM-L6-v2 ~80MB lần đầu)
chromadb>=0.5         # vector store local bền (PersistentClient)
# Ghi chú: nếu KHÔNG cài 2 gói dưới, _backend.py tự lùi về hashing-embedder + JSON store.
